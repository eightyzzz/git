# STM32 天气时钟 项目交接文档

> 用途：供新对话/开发者快速接续本项目。
>
> 最近更新：2026-09-12（补充 MTP 电脑端重命名、设备内复制/移动、枚举缓存的实现要点；刷新 Flash/RAM 数字）。

## 1. 项目目标

基于 STM32F407VET6（512KB Flash / 128KB 链接 RAM + 64KB CCM 未用）+ FreeRTOS + LVGL 的桌面天气时钟：

- WiFi 联网（ESP32-C3 AT）获取天气与 SNTP 时间，主页显示时间/天气/室内温湿度/天气图标。
- 240x320 ST7789 屏幕 + FT6336 电容触摸，第二页提供 WiFi 设置和图片文件查看。
- W25Q128 x2 大容量 Flash 存字库/图标/图片，USB MTP 让电脑直接拖文件进 Flash。

## 2. 目录结构

```
项目根
├─ app/              应用层
│  ├─ main.c         入口，创建 init 任务(prio 9)
│  ├─ app.c          业务任务/定时器：WiFi、SNTP 校时、天气、室内外刷新
│  ├─ board.c        外设时钟/引脚初始化
│  ├─ lvgl_ui.c/h    全部 UI（主页/第二页/WiFi 设置/文件列表/图片预览）
│  ├─ lv_port_disp.c 显示刷新（DISP_BUF_ROWS=10 双缓冲，共 9600B，异步 DMA flush）
│  ├─ lv_port_indev.c 触摸输入
│  ├─ wifi.c/h       ESP32-C3 AT 联网；wifi.h 有默认 SSID/密码
│  ├─ weather.c/h    心知天气响应解析
│  ├─ workqueue.c/h  任务间回调队列
│  ├─ font/lv_font_time_68.c  时间大字体（编译进固件）
│  ├─ font/lv_font_cn.c       中文字库（编译进固件，不再从 flash 加载）
│  └─ storage/       FAL/FlashDB(分区KV) + littlefs + lv_fs_lfs(A:/ 映射)
├─ driver/           外设驱动：st7789/ft6336/aht20/espat/spi_flash/usb_mtp/rtc/console/tim_delay
├─ firmware/         CMSIS 设备头
├─ mdk/              Keil MDK 工程（stm32f407.uvprojx，Listings/Objects）
├─ LVGL/             拖进 W25Q128 的资源（font_cn.fnt、icon_*.bin）
├─ resources/        字库/图标的仓库副本（与 LVGL/ 同步）
├─ scripts/          gen_resources.ps1 等（注意：图标生成脚本已过时，见下）
├─ third_lib/        lvgl 8.3.11、littlefs、FlashDB、FAL 等
└─ 原理图.pdf        硬件参考
```

## 3. 已完成功能

- **主页**：时间（SNTP 校时 + RTC）、天气（心知天气 API）、室内温湿度（AHT20）、天气图标与 WiFi 状态图标（从 Flash 加载）。
- **WiFi**：ESP32-C3 AT 联网；第二页 WiFi 设置：名称/密码输入（键盘仅英数）、输入光标闪烁、密码可见/隐藏切换、保存后自动重连（存 FlashDB）、退出设置自动隐藏密码、键盘收起时光标消失。
- **文件/图片查看**：支持**子目录浏览**（文件夹排前、`.bin` 图片在后；虚拟列表：只建 8 行控件滚动复用 + 静态 128 项索引，文件再多不耗 LVGL 堆；过滤 littlefs 合成的 `.` / `..` 条目；返回按钮在子目录退上一级、根目录回第二页）；点选后整屏显示图片——用 `lv_img_set_src(文件路径)` 让 LVGL 内建解码器**逐行流式读取**（支持 240x320 且不占大 RAM）；左右滑动切上一张/下一张；右下角半透明返回键（透明度 10）。
- **MTP USB**：PA12/PA11 → CH334，VID_0483/PID_5741，WPD MTP 设备，可向 W25Q128 拖文件，**支持新建文件夹、向子目录拖文件、设备内复制/移动/改名文件与文件夹**（CopyObject 0x1017 / MoveObject 0x1018 / SetObjectPropList 0x9806）。
  - 句柄 = 全路径 FNV-1a 哈希。改名会让句柄改变，因此另有一张 16 槽「旧句柄 → 新路径」映射表（`mtp_rename`，命中即消费，会话打开时清空）。
  - 打开盘符卡顿已解决：目录枚举时把句柄→路径写进缓存（`MTP_CACHE_MAX=48` + 1.5KB 路径池），查询先查缓存，避免整树扫描 O(N²) 读 SPI Flash。
  - Copy/Move 源与目标同路径时必须直接返回原句柄，否则 TRUNC 打开会清空源文件。
  - usb_mtp.c 只保留错误/启动日志（25 处 printf），高频调试日志已删。
- **存储**：FlashDB KV（WiFi 配置）+ littlefs 文件系统（LVGL 访问用 `A:/` 前缀）。
- **字库**：中文字库已**编译进固件**（`app/font/lv_font_cn.c`，simhei 20px、bpp4、无压缩、无 kern；ASCII + 43 个 UI 汉字，含“厦门”）；时间大字体 `lv_font_time_68.c` 编译进固件。`LVGL/font_cn.fnt` 仅是备份，固件不再读取，可删。
- **图标**：`icon_*.bin` 读入 RAM 显示（天气 54x54、WiFi 24x24）——因为 LVGL 软件渲染不支持“文件图+zoom”。

## 4. 正在开发

- 暂无进行中需求（2026-08-30 完成：文件管理支持子目录浏览 + 虚拟列表；MTP 电脑端重命名、设备内复制/移动文件夹、打开盘符卡顿优化、日志清理）。
- 两指缩放查看图片——**已放弃**：240x320 真彩图 150KB 超 RAM，且 LVGL 8.3 文件图不支持 zoom。

## 5. 关键技术栈

- STM32F407VET6（Cortex-M4，512KB Flash；SRAM 共 192KB = 0x20000000 的 128KB + 0x10000000 的 64KB CCM，链接区只用前 128KB），Keil MDK（ARMCC）。
- FreeRTOS；LVGL **8.3.11**（`lv_conf.h`：LV_COLOR_DEPTH=16、LV_COLOR_16_SWAP=0、LV_MEM_SIZE=28KB）。
- 外设：ST7789(240x320)、FT6336 触摸、AHT20(I2C)、W25Q128 x2(SPI，FAL+FlashDB V2.2.0+littlefs)、USB OTG FS MTP、ESP32-C3 AT 固件。

## 6. 重要文件说明

| 文件 | 作用 |
|---|---|
| app/lvgl_ui.c | 全部 UI；城市显示映射（Longyan→龙岩、Xiamen→厦门）；文件流式预览；滑动手势 |
| app/app.c | 业务调度；天气 URL `location=xiamen`（**用户自行修改城市**） |
| app/wifi.h | 默认 WIFI_SSID/WIFI_PASSWD |
| app/storage/lv_fs_lfs.c、storage.c | littlefs 与 `A:/` 映射、FlashDB KV |
| driver/usb_mtp.c | MTP 实现（op 分发、句柄=全路径 FNV-1a 哈希、枚举缓存、改名映射表、Copy/Move） |
| app/lv_port_disp.c | 显示刷新，DISP_BUF_ROWS=10 双缓冲（2×10 行共 9600B）+ 异步 DMA flush |
| app/font/lv_font_cn.c | 中文字库（编译进固件；LVGL/ 与 resources/ 的 font_cn.fnt 仅备份，不再读取） |
| mdk/stm32f407.uvprojx | Keil 工程 |

## 7. 已知问题

- **RAM**：最新完整编译 RW-data 1132 + ZI-data 117028 = 118160 B ≈ 115.4KB / 128KB，余量约 12.6KB；LV_MEM_SIZE=28KB（中文字库已编进固件，不再运行时加载）。MTP 的枚举缓存（48×16B + 1.5KB 池 ≈ 2.3KB）与改名映射表（16×108B ≈ 1.7KB）都在 ZI 段；真缺 RAM 时按 `MTP_CACHE_MAX`（48→24）→ `MTP_RENAME_MAX`（16→8）→ `MTP_CACHE_POOL`（1536→768）的顺序砍，代价分别是文件多时打开盘符变慢、连续改名次数变少。还有 64KB CCM（0x10000000）未用，可放任务栈/LVGL 堆（不可放 DMA 缓冲）。
- **Flash**：Code 226420 + RO-data 217032 + RW-data 1132 = 444584 B ≈ 434.2KB / 512KB，余量约 77.8KB。
- **显示**：ST7789 SPI 已提到 21MHz（超 ST7789 标称 15.5MHz，实测稳定）；若花屏把 st7789.c 的 `SPI_BaudRatePrescaler_2` 改回 `_4`。当前为双缓冲异步 flush，可能出现扫描撕裂线（无 TE 引脚，软件无法根除；回退单缓冲可恢复）。
- 图片不能两指缩放（见“正在开发”）。
- 正在预览/刚打开过的文件，MTP 删除可能失败或设备断开（解码器占用文件）。
- 控制台中文日志是 UTF-8，串口工具按 GBK 看会乱码（仅日志，不影响功能）。
- 日志有 “invaid date fotmate” 拼写（SNTP 解析失败时打印，无害）。
- 文件列表与滑动切换按名称排序（文件夹、图片各自排序；自然数字序，如 1、2、10；大小写不敏感）。
- `scripts/gen_resources.ps1` 已过时（它读 `app/image/*.c`，该目录已删除；图标/字库现在直接放 LVGL/ 目录）。

## 8. 不能改动 / 需要注意

- **USB 引脚已焊死**：D+/D- 固定 PA12/PA11 → CH334 D3，MTP 相关代码/引脚不要动。
- **MTP 能力声明**：GetObjectPropDesc（0x9802）里的 GetSet 是枚举（0x00 只读 / 0x01 可读可写 / 0x02 只写），不是位域——ObjectFileName(0xDC07)、Name(0xDC44) 必须是 0x01，写成 0x02 或 0x03 会让 Windows 把「重命名」变灰。ObjectFormat 全路径统一：文件 0x3000、文件夹 0x3001（取值不一致会导致属性表构建失败）。
- **改完 MTP 固件必须拔插 USB**：Windows 会缓存 WPD 设备能力（能否改名等），不重新枚举会看到旧行为。
- **句柄是路径哈希**：任何让对象路径变化的操作（改名/移动）都要补一条 `mtp_rename_add` 映射并清缓存，否则 Windows 用旧句柄回查会拿到 0x2009，表现为设备掉成磁盘。
- **字节序**：LV_COLOR_16_SWAP=0，所有 `.bin`（字库/图标/图片）都是 RGB565 **小端**（低字节在前），4 字节头 = `4 | (w<<10) | (h<<21)`。
- **图片查看方式**：保持 `lv_img` + 文件源流式显示（240x320 可行）；不要改回读整图进 RAM；不要依赖 `lv_img_set_zoom`（文件图不支持）。
- 预览文件路径字符串要常驻（`static file_preview_path`），`lv_img_set_src` 只保存指针。
- 中文 UI 字符串全部用 `\x` 十六进制转义（避免 ARMCC/GBK 编码问题），新加中文必须照此格式。
- **字库重新生成**：必须覆盖全部 UI 汉字（从 lvgl_ui.c 的 STR_CN_* 提取，当前 43 个，含“厦门”）；命令（lv_font_conv）：`--size 20 --bpp 4 --format lvgl --no-compress --no-prefilter --no-kerning --font simhei.ttf --range 0x20-0x7F --symbols "全部汉字" -o app/font/lv_font_cn.c`；加入 Keil 工程（app/font 组），工程 define 需含 `LV_LVGL_H_INCLUDE_SIMPLE`。LVGL/ 与 resources/ 的 .fnt 仅备份，无需同步。
- 天气接口城市（`app.c` URL 的 `location=`）由用户自改；代码只负责 Longyan→龙岩、Xiamen→厦门 的显示映射，不要做其它随意映射。
- 改 littlefs/FlashDB 分区布局会破坏 W25Q128 现有数据。
- 已知设计坑：错误页/高优先级(9)上下文里队列发消息后死循环等 UI 任务(prio 8)会导致 UI 卡住——涉及 UI 同步的改动注意优先级与回调。
